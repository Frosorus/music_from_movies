# Main pipeline env — steps 0, 1, 3, 4 in pipeline.sh
# (yt-dlp, Demucs, librosa, MoviePy, video assembly)
#
# Versions below are the latest PyPI releases found by pipreqs.
# To pin to your tested versions, run in your working conda env:
#   pip freeze | grep -E "demucs|librosa|Metafone|moviepy|numpy|scipy|torch|tqdm|yt.dlp"

yt-dlp
demucs==4.0.1
librosa==0.11.0
Metafone==0.5
moviepy==2.2.1
numpy==2.4.4
scipy==1.17.1
torch==2.11.0
tqdm==4.67.3
